# -*- mode: python -*-

block_cipher = None


a = Analysis(['launch.py'],
             pathex=['C:\\Users\\C245895\\AppData\\Local\\Continuum\\Anaconda3\\Lib\\site-packages\\PyQt5\\Qt\\bin', 'C:\\Users\\C245895\\PycharmProjects\\TractaViewer'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='launch',
          debug=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='launch')
